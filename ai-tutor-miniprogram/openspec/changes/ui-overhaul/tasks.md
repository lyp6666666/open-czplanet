## 1. 全局样式 (Global Styling)

- [ ] 1.1 更新 `uni.scss` 使用新的颜色变量 (--primary, --bg, --card, --text, --muted, --border)。
- [ ] 1.2 更新 `App.vue` 应用全局背景色。
- [ ] 1.3 配置 `pages.json` 的 `tabBar` 使用新配色方案（选中颜色: #00bebd, 默认颜色: #646a73）。

## 2. 登录页面重设计 (Login Page Redesign)

- [ ] 2.1 重构 `pages/me/index.vue`，为未登录状态实现专门的登录卡片布局。
- [ ] 2.2 样式化登录输入框（手机号、验证码），包括圆角边框和聚焦状态。
- [ ] 2.3 样式化登录按钮（发送验证码、登录、微信登录），使其匹配 Web 应用的主按钮风格。
- [ ] 2.4 添加登录流程的验证反馈和加载状态。

## 3. 页面样式更新 (Page Styling Updates)

- [ ] 3.1 更新 `pages/home/index.vue` 使用新背景和卡片样式。
- [ ] 3.2 更新 `pages/chat/list.vue` 以符合新设计系统。
- [ ] 3.3 更新 `pages/me/index.vue` 认证后的视图，使用新的个人资料头部和按钮样式。
