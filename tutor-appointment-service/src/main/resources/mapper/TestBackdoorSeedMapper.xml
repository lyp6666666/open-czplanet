<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.tutor.appointment.mapper.TestBackdoorSeedMapper">

    <insert id="upsertUser">
        INSERT INTO user (id, name, phone, avatar, sex, status, user_type, ref_id, create_time, update_time)
        VALUES (#{id}, #{name}, #{phone}, #{avatar}, #{sex}, 0, #{userType}, #{refId}, NOW(3), NOW(3))
        ON DUPLICATE KEY UPDATE
            name = VALUES(name),
            phone = VALUES(phone),
            avatar = VALUES(avatar),
            sex = VALUES(sex),
            status = VALUES(status),
            user_type = VALUES(user_type),
            ref_id = VALUES(ref_id),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertTeacherProfile">
        INSERT INTO teacher_profile (
            id, user_id, real_name, education, subject, experience_years, rate_per_hour, introduction,
            city, highest_edu_school, teaching_mode, resume_completed, certificate_urls, status, create_time, update_time
        )
        VALUES (
            #{id}, #{userId}, '后门教师', '测试 本科', '初中数学', 3, 199.00, '用于联调支付权限的测试教师账号。',
            '北京', '测试大学', 'ONLINE', 1, '[]', 1, NOW(3), NOW(3)
        )
        ON DUPLICATE KEY UPDATE
            user_id = VALUES(user_id),
            status = VALUES(status),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertStudentProfile">
        INSERT INTO student_profile (id, user_id, real_name, age, address, demand_description, budget, status, create_time, update_time)
        VALUES (#{id}, #{userId}, '测试学生', 13, '北京市海淀区', '用于联调支付权限的测试学生账号。', 120.00, 1, NOW(3), NOW(3))
        ON DUPLICATE KEY UPDATE
            user_id = VALUES(user_id),
            status = VALUES(status),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertStudentJobPosting">
        INSERT INTO student_job_posting (
            id, parent_id, subject_name, subject_is_other, title, description, student_gender,
            teacher_gender_preference, teacher_requirement_detail, class_mode, frequency_per_week,
            publisher_identity, budget_min, budget_max, stage_code, education_requirement, schedule,
            biz_status, status, create_time, update_time
        )
        VALUES (
            #{id}, #{parentId}, '数学', 0, '测试需求（后门教师支付联调）', '用于联调：学生已同意申请，教师侧应展示信息费支付卡片。',
            'male', 'both', '希望老师讲解清晰、按时上课。', 'online', 2, 'PARENT', 80, 120, 'PRIMARY', 'UNLIMITED',
            JSON_ARRAY('Tue 19-21'), 2, 1, NOW(3), NOW(3)
        )
        ON DUPLICATE KEY UPDATE
            parent_id = VALUES(parent_id),
            biz_status = VALUES(biz_status),
            status = VALUES(status),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertRoom">
        INSERT INTO room (id, teacher_profile_id, student_profile_id, active_time, last_msg_id, status, create_time, update_time)
        VALUES (#{id}, #{teacherProfileId}, #{studentProfileId}, NOW(3), #{lastMsgId}, 1, NOW(3), NOW(3))
        ON DUPLICATE KEY UPDATE
            teacher_profile_id = VALUES(teacher_profile_id),
            student_profile_id = VALUES(student_profile_id),
            active_time = VALUES(active_time),
            last_msg_id = VALUES(last_msg_id),
            status = VALUES(status),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertTutorApplication">
        INSERT INTO tutor_application (
            id, sender_uid, receiver_uid, sender_role, receiver_role, context_type, context_id,
            content, client_request_id, status, chat_access_status, room_id, receiver_read, decided_at, create_time, update_time
        )
        VALUES (
            #{id}, #{senderUid}, #{receiverUid}, 'TEACHER', 'STUDENT', 'DEMAND', #{contextId},
            '您好，我看了您的需求，和我的授课方向非常匹配，我们可以进一步详细沟通吗？',
            'backdoor-seed-666002', 'ACCEPTED', 'PAYMENT_REQUIRED', #{roomId}, 1, NOW(3), NOW(3), NOW(3)
        )
        ON DUPLICATE KEY UPDATE
            sender_uid = VALUES(sender_uid),
            receiver_uid = VALUES(receiver_uid),
            status = VALUES(status),
            chat_access_status = VALUES(chat_access_status),
            room_id = VALUES(room_id),
            receiver_read = VALUES(receiver_read),
            decided_at = VALUES(decided_at),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertBrokerageOrder">
        INSERT INTO brokerage_order (
            id, proposal_id, application_id, room_id, payer_uid, amount_fen, pay_method, status,
            proof_url, proof_note, paid_at, create_time, update_time
        )
        VALUES (
            #{id}, NULL, #{applicationId}, #{roomId}, #{payerUid}, 1, NULL, 'PENDING',
            NULL, NULL, NULL, NOW(3), NOW(3)
        )
        ON DUPLICATE KEY UPDATE
            application_id = VALUES(application_id),
            room_id = VALUES(room_id),
            payer_uid = VALUES(payer_uid),
            amount_fen = VALUES(amount_fen),
            status = VALUES(status),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertApplicationBrokerageOrder">
        INSERT INTO application_brokerage_order (id, application_id, order_id, create_time, update_time)
        VALUES (#{id}, #{applicationId}, #{orderId}, NOW(3), NOW(3))
        ON DUPLICATE KEY UPDATE
            application_id = VALUES(application_id),
            order_id = VALUES(order_id),
            update_time = VALUES(update_time)
    </insert>

    <insert id="upsertMessage">
        INSERT INTO message (id, room_id, from_uid, to_uid, content, reply_msg_id, status, gap_count, type, extra, create_time, update_time)
        VALUES (#{id}, #{roomId}, #{fromUid}, #{toUid}, #{content}, NULL, 0, NULL, 8, CAST(#{extra} AS JSON), NOW(3), NOW(3))
        ON DUPLICATE KEY UPDATE
            room_id = VALUES(room_id),
            from_uid = VALUES(from_uid),
            to_uid = VALUES(to_uid),
            content = VALUES(content),
            status = VALUES(status),
            type = VALUES(type),
            extra = VALUES(extra),
            update_time = VALUES(update_time)
    </insert>

</mapper>
