## 1. 数据库与领域模型

- [ ] 1.1 扩展 `brokerage_order` 状态枚举与相关 SQL（兼容现有数据）
- [ ] 1.2 新增 `refund_request` 表与索引（聊天退款/试课退款统一承载）
- [ ] 1.3 新增 `payment_refund` 表与索引（支付域退款幂等与审计）
- [ ] 1.4 新增 `course_enrollment` 表与索引（驱动“我的课程”状态视图）

## 2. videoCall-IM-service（用户侧与课程域）

- [ ] 2.1 新增「聊天退款状态查询」与「申请聊天退费」接口（校验：已支付/未合作/无重复申请）
- [ ] 2.2 申请聊天退费后发送系统消息 `BROKERAGE_REFUND_REQUEST` 并关闭 room（复用现有 closeRoom 逻辑）
- [ ] 2.3 合作达成后仍允许聊天：修正 ChatController 的前置校验逻辑并补回归测试
- [ ] 2.4 扩展联系方式解锁：支持双方查看对方电话（proposal=ACCEPTED + order=PAID + 无退款中/已退费）
- [ ] 2.5 新增 course_enrollment 生成与状态推进逻辑（申请→待支付→沟通→试课→开课）
- [ ] 2.6 新增「我的课程」列表接口（聚合对方信息摘要、状态、试课截止时间）
- [ ] 2.7 新增「试课不通过退款申请（60%）」接口（说明+图片证据校验）
- [ ] 2.8 为关键接口补齐 Swagger 注释、参数校验与错误码（含并发/幂等处理）

## 3. payment-service（YunGouOS 原路退款闭环）

- [ ] 3.1 扩展 `YungouosClient` 支持退款调用（mock:// 走本地 mock）
- [ ] 3.2 新增退款应用服务：创建/复用 payment_refund，幂等、金额校验、失败重试策略
- [ ] 3.3 新增内部退款接口 `POST /payment/refund`（仅服务间调用，带严格注释与校验）
- [ ] 3.4 补齐单元测试/集成测试（成功/失败/重复审批/部分退款 60%）

## 4. ai-tutor-admin（管理端退款审核）

- [ ] 4.1 新增退款申请列表/详情/通过/拒绝 API（替换或兼容现有 disputes API）
- [ ] 4.2 详情页：展示聊天记录（不脱敏）+ 试课说明 + 图片证据
- [ ] 4.3 审核通过：调用 payment-service 退款接口，成功后更新 refund_request/brokerage_order/course_enrollment 状态
- [ ] 4.4 审核拒绝：记录原因并回退到可继续状态（沟通中/试课中/开课中按规则回退）
- [ ] 4.5 为审核链路补齐测试（mock 下游退款，验证幂等与状态机）

## 5. ai-tutor-web（用户侧 UI）

- [ ] 5.1 聊天页新增“申请退费”按钮 + tooltip（可用/禁用两套文案）
- [ ] 5.2 合作达成后弹出联系方式（双方），并禁用“申请退费”按钮
- [ ] 5.3 新增“我的课程”页与顶部导航入口
- [ ] 5.4 课程列表按状态展示，并提供教师侧“试课不通过”申请入口（说明+图片上传）
- [ ] 5.5 前端自测：接口连通、状态切换、按钮禁用态与提示文案正确

## 6. 验收与回归

- [ ] 6.1 E2E 流程：支付→沟通→聊天退款申请→管理端审核→原路退款→订单/课程状态收敛
- [ ] 6.2 E2E 流程：支付→合作提案→双方同意→展示联系方式→进入试课→试课退款申请→管理端审核→60% 退款
- [ ] 6.3 关键回归：未支付不可聊天、合作达成后仍可聊天、退款申请后立即关闭聊天、重复操作幂等

