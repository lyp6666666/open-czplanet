## 1. 数据库迁移与兼容

- [ ] 1.1 为 `room_read_state` 增加幂等建表迁移脚本并接入迁移流程
- [x] 1.1 为 `room_read_state` 增加幂等建表迁移脚本并接入迁移流程
- [x] 1.2 为教师档案新增字段（city/school/teaching_mode 等）编写迁移脚本
- [x] 1.3 更新测试用 schema.sql/初始化脚本以覆盖新表与新字段

## 2. 后端：已读持久化与未读一致性（videoCall-IM-service）

- [x] 2.1 调整 ACK 已读逻辑：移除运行时建表依赖，写入失败返回明确错误
- [x] 2.2 校验会话未读数计算与 read_state 关联逻辑，补齐边界（空状态/乱序 ACK）
- [x] 2.3 增加关键链路测试：ACK 成功后会话未读数归零、刷新后仍为 0

## 3. 前端：实时消息主动渲染（ai-tutor-web）

- [x] 3.1 统一 SSE 使用：收敛聊天页重复 stream 连接到全局实时 store
- [x] 3.2 会话列表实时更新：基于 message 事件更新预览/时间/排序与未读数
- [x] 3.3 ACK 与未读校准：在进入会话、重连成功等时机进行校准，避免红点反复

## 4. 后端：修改手机号流程（tutor-appointment-service）

- [x] 4.1 设计并实现“两段验证码”接口（旧号校验 + 新号验证码）并保持旧接口兼容
- [x] 4.2 增加新手机号占用校验、格式校验与必要的频控策略
- [x] 4.3 修改手机号成功后的会话安全策略落地（清理 token/要求重登或刷新 token）

## 5. 前端：修改手机号页面与入口（ai-tutor-web）

- [x] 5.1 在“我的/设置”新增修改手机号入口（教师/学生共用）
- [x] 5.2 实现修改手机号两步交互（发送验证码、校验、错误提示、成功态）
- [x] 5.3 修改成功后的登录态处理（引导重新登录或自动刷新凭证）

## 6. 后端：家教两步入驻与简历字段扩展（tutor-appointment-service）

- [x] 6.1 扩展 `updateUserInfo`/`me` 等接口支持新增简历字段读写
- [x] 6.2 将“入驻完成状态”落到服务端字段（第一步必填、第二步可选）并定义判断规则
- [x] 6.3 增加教师档案保存/回显测试与字段兼容测试

## 7. 前端：家教首次入驻两步流程（ai-tutor-web）

- [x] 7.1 新增/改造入驻第一步页面（头像上传 + 真实姓名）并接入提交流程
- [x] 7.2 新增/改造入驻第二步页面（学历/城市/学校/介绍/学科多选/教学方式）并支持跳过
- [x] 7.3 更新路由守卫与“简历/我的”编辑页以支持新字段回显与后续补齐

## 8. 模拟数据与端到端验证

- [x] 8.1 更新 `seed_dev_data.sql` 与前端 MSW mocks，覆盖新字段与新流程
- [x] 8.2 增加端到端验证脚本/用例：消息实时到达、已读后刷新不再红、修改手机号、两步入驻
