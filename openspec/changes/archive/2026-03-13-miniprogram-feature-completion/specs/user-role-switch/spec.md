## 新增需求 (ADDED Requirements)

### 需求：用户角色切换
系统应支持用户在家长和教师角色之间切换，并动态渲染首页。

#### 场景：双重身份登录
- **当** 具有双重身份（家长/教师）的用户登录时
- **那么** 系统应默认进入上次使用的角色
- **且** 在“我的”页面提供“切换身份”按钮

#### 场景：角色引导
- **当** 新用户（默认学生角色）登录并点击“成为教师”时
- **那么** 系统应引导用户去 Web 端完成入驻（或展示简易入驻页）
- **且** 提交入驻后，角色状态变更为“审核中”
